# vim: set ft=ansible:
- hosts: ec2generator
  gather_facts: false
  user: ubuntu
  vars:
    dest_directory: "./{{ workload }}_threads_{{ threads }}_{{ date }}"

  tasks:
    - include: test.yml
      vars:
        dest_directory: "./{{ workload }}_threads_{{ item }}_{{ date }}"
        db: postgresql
        db_parameters: pgjsonb -s -cp bindings/lib/postgresql-9.4.1207.jar
      with_items: "{{ thread_list }}"
